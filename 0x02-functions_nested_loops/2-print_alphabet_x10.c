#include "main.h"

/**
 * print_alphabet - print alphabet 10 times
 *
 * Return: Always 0.
 */

void print_alphabet_x10(void)
{
int count = 0;
char letter;

while (counter++ <= 9)
{
for (letter = 'a'; letter <= 'z'; letter++)
_putchar(letter);
_putchar('\n');
}
}
