#include "main.h"
#include <stdio.h>

/**
 * _abs - value absolute
 *
 * @n: The int to be printed
 * Return: the absolute value of the integer.
 */

_abs(int n)

{
if (n >= 0)
return (n);
else
return (-n);
}
