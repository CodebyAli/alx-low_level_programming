#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry point
*
*Return: 0
*/
int main(void)
{
	char alphabet;

	for (c = 'a'; c <= 'z'; alphabet++);
	putchar(alphabet);

	putchar('\n');

	return (0);
}
