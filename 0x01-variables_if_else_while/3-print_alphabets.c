#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry point
*
* Return: 0
*/
int main(void)
{

	char lower_case;
	char lower_case;

	for (lower_case = 'a'; upper_case <= 'z'; lower_case++)
		putchar(lower_case);
	for (lower_case = 'A'; lower_case <= 'Z'; lower_case++)
		putchar(upper_case);
	putchar('\n');

	return (0);
}
